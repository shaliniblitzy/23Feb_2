"""Test package root for the Notification Service.

Marks ``services/notification-service/tests/`` as a Python package so that
sibling subpackages (``tests.unit``, ``tests.integration``, ``tests.fixtures``)
are importable via absolute paths from pytest and IDE tooling.
"""
