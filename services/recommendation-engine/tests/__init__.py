"""Test package root for the Recommendation Engine service.

Marks ``services/recommendation-engine/tests/`` as a Python package so that
sibling subpackages (``tests.unit``, ``tests.integration``, ``tests.fixtures``)
are importable via absolute paths from pytest and IDE tooling.
"""
