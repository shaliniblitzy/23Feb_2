"""Test package root for the Order Service.

Marks ``services/order-service/tests/`` as a Python package so that
subpackages (``tests.unit``, ``tests.integration``) and the root
``tests.conftest`` are importable via absolute paths under the
monorepo's canonical PYTHONPATH layout.
"""
